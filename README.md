# SimpleStore-Backend-Go
A restful api backend of a simple online store. A project from a course.


# Build and launch
    docker-compose down -v && docker-compose build --no-cache && docker-compose up

# lfs files download
    sudo apt install git-lfs
    git lfs pull
